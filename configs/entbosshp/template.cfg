"bosses"
{
	"0"
	{
		"name"			"" // Boss Name
		"type"			"" // breakable, math_counter, hpbar
		"entity"		"" // entity name
		"countermode"	"" // 1 = OnHitMin| 2 = OnHitMax (MathCounter and HPBar Only)
		"iterator"		"" // iterator entity name (HPBar Only)
		"iteratormode"  "" // 1 = OnHitMin| 2 = OnHitMax (HPBar Only)
		"backup"		"" // backup entity name (HPBar Only)
	}
}